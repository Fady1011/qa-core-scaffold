# Mobile Automation Template

Kick-start WebdriverIO + Appium mobile suites with shared `qa-core` building blocks.

## Quickstart
1. Install dependencies and Appium server.
2. Configure desired capabilities via `.env`.
3. Run `npm run test:mobile` to execute the WDIO suite.
