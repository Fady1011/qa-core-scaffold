# Web Automation Template

This template provides a Playwright starter project wired with `@yourorg/qa-core`.

## Getting Started
1. Install dependencies: `npm install`.
2. Run web tests: `npm run test:web`.
3. Generate report: `npm run qa:report`.
