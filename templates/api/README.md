# API Automation Template

Bootstrap API contract tests using `@yourorg/qa-core` clients and validators.

## Workflow
- Configure environment variables in `.env.qa`.
- Use `ApiClient` for REST calls and `SchemaValidator` for contract validation.
- Execute suites with `npm run test:api`.
